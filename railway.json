{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd backend && npm ci --omit=dev && npm run build && npm start",
    "restartPolicyMaxRetries": 5
  }
}
